{
  "name": "xr-stage-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "WebXR spatial stage — four-mode (flat/desktop/VR/AR) rendered room + LiveKit voice. Foundation only.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "livekit-client": "^2.7.0",
    "three": "^0.167.0"
  },
  "devDependencies": {
    "@vitejs/plugin-basic-ssl": "^1.1.0",
    "vite": "^5.4.0"
  }
}
